syntax = "proto3";
package chat;

service ChatService {
  rpc Chat(stream ChatMessage) returns (stream ChatReply);
}

message ChatMessage {
  string name = 1;
  string message = 2;
}

message ChatReply {
  string name = 1;
  string message = 2;
}